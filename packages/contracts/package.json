{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "chain": "hardhat node",
    "deploy": "hardhat compile && node --env-file-if-exists=.env scripts/deploy.ts localhost",
    "deploy:sepolia": "hardhat compile && node --env-file-if-exists=.env scripts/deploy.ts baseSepolia",
    "fund": "node scripts/fund.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24",
    "hardhat": "^3",
    "typescript": "^5",
    "viem": "^2"
  }
}
