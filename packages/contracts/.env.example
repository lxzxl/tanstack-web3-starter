# Deploy to a public testnet (e.g. Base Sepolia: `pnpm deploy:sepolia`).
# Use a TESTNET-ONLY key — NEVER one that holds real funds.
# Fund it from a faucet first, e.g. https://www.base.org/build/faucet
DEPLOYER_PRIVATE_KEY=

# Optional — override the default public RPC.
BASE_SEPOLIA_RPC_URL=
